apiVersion: v1
kind: Service
metadata:
  name: devops-nodejs-service
spec:
  type: LoadBalancer
  selector:
    app: devops-nodejs-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000